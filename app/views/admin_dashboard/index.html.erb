<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Railquill Admin Dashboard</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <script src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
  <style>
    body { font-family: 'Inter', sans-serif; }
    
    /* Custom Design System Variables */
    :root {
      --primary-50: #EEF2FF;
      --primary-100: #E0E7FF;
      --primary-500: #6366F1;
      --primary-600: #4F46E5;
      --primary-700: #4338CA;
      --success: #059669;
      --success-light: #D1FAE5;
      --warning: #D97706;
      --warning-light: #FEF3C7;
      --error: #DC2626;
      --info: #2563EB;
      --info-light: #DBEAFE;
    }
    
    /* Glass morphism effect */
    .glass {
      background: rgba(255, 255, 255, 0.7);
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.18);
    }
    
    /* Enhanced card hover effect */
    .metric-card {
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }
    
    .metric-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
    }
    
    /* Activity timeline */
    .timeline-item::before {
      content: '';
      position: absolute;
      left: 1rem;
      top: 2.5rem;
      bottom: -1rem;
      width: 2px;
      background: linear-gradient(to bottom, #E5E7EB, transparent);
    }
    
    .timeline-item:last-child::before {
      display: none;
    }
    
    /* Gradient backgrounds */
    .gradient-primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }
    
    .gradient-success {
      background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
    }
    
    .gradient-warning {
      background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
    }
    
    .gradient-info {
      background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
    }
    
    /* Animations */
    @keyframes fadeInUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    .animate-fade-in-up {
      animation: fadeInUp 0.6s ease-out;
    }
    
    /* Custom scrollbar */
    .custom-scrollbar::-webkit-scrollbar {
      width: 6px;
    }
    
    .custom-scrollbar::-webkit-scrollbar-track {
      background: #f1f1f1;
      border-radius: 10px;
    }
    
    .custom-scrollbar::-webkit-scrollbar-thumb {
      background: #c1c1c1;
      border-radius: 10px;
    }
    
    .custom-scrollbar::-webkit-scrollbar-thumb:hover {
      background: #a8a8a8;
    }
  </style>
</head>
<body class="h-full bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50">
  <div class="flex h-full" x-data="{ sidebarOpen: false }">
    <!-- Enhanced Sidebar -->
    <div class="hidden lg:flex lg:w-64 lg:flex-col">
      <div class="flex flex-col flex-grow glass border-r border-white/20 pt-5 overflow-y-auto shadow-xl">
        <div class="flex items-center flex-shrink-0 px-6 pb-6">
          <div class="flex items-center space-x-3">
            <div class="w-10 h-10 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-xl flex items-center justify-center shadow-lg">
              <span class="text-white font-bold text-lg">R</span>
            </div>
            <div>
              <h1 class="text-xl font-bold text-gray-900">Railquill</h1>
              <p class="text-xs text-gray-500">Admin Dashboard</p>
            </div>
          </div>
        </div>
        
        <!-- Search Bar -->
        <div class="px-4 mb-6">
          <div class="relative">
            <input type="text" placeholder="Quick search..." 
                   class="w-full pl-10 pr-4 py-2 text-sm border border-gray-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent bg-white/50">
            <div class="absolute inset-y-0 left-0 pl-3 flex items-center">
              <%= heroicon_tag "magnifying-glass", size: 20, class: "text-gray-400" %>
            </div>
          </div>
        </div>
        
        <div class="mt-2 flex-grow flex flex-col">
          <nav class="flex-1 px-4 space-y-2">
            <a href="/admin_dashboard" class="bg-white/60 text-indigo-700 group flex items-center px-4 py-3 text-sm font-medium rounded-xl border-l-4 border-indigo-500 shadow-sm">
              <%= heroicon_tag "squares-2x2", size: 20, class: "mr-3" %>
              Dashboard
            </a>
            <a href="/admin/posts" class="text-gray-700 hover:bg-white/40 hover:text-indigo-600 group flex items-center px-4 py-3 text-sm font-medium rounded-xl transition-all duration-200">
              <%= heroicon_tag "pencil-square", size: 20, class: "mr-3" %>
              Posts
              <% if @draft_posts > 0 %>
                <span class="ml-auto bg-yellow-100 text-yellow-600 text-xs font-medium px-2 py-1 rounded-full"><%= @draft_posts %></span>
              <% end %>
            </a>
            <a href="/admin/admin_users" class="text-gray-700 hover:bg-white/40 hover:text-indigo-600 group flex items-center px-4 py-3 text-sm font-medium rounded-xl transition-all duration-200">
              <%= heroicon_tag "user-group", size: 20, class: "mr-3" %>
              Admin Users
            </a>
            <a href="/" target="_blank" class="text-gray-700 hover:bg-white/40 hover:text-indigo-600 group flex items-center px-4 py-3 text-sm font-medium rounded-xl transition-all duration-200">
              <%= heroicon_tag "arrow-top-right-on-square", size: 20, class: "mr-3" %>
              View Site
            </a>
          </nav>
          
          <!-- User Profile Section -->
          <div class="px-4 pb-6 border-t border-white/20 pt-6 mt-6">
            <div class="flex items-center space-x-3 p-3 rounded-xl bg-white/40">
              <div class="w-10 h-10 bg-gradient-to-br from-purple-500 to-pink-500 rounded-full flex items-center justify-center text-white font-semibold text-sm">
                <%= current_admin_user.email.first.upcase %>
              </div>
              <div class="flex-1 min-w-0">
                <p class="text-sm font-medium text-gray-900 truncate">
                  <%= current_admin_user.email.split('@').first.capitalize %>
                </p>
                <p class="text-xs text-gray-500">Administrator</p>
              </div>
            </div>
            
            <%= link_to destroy_admin_user_session_path, method: :delete, 
                        class: "mt-3 w-full text-gray-700 hover:bg-red-50 hover:text-red-600 group flex items-center px-4 py-3 text-sm font-medium rounded-xl transition-all duration-200" do %>
              <%= heroicon_tag "arrow-right-on-rectangle", size: 20, class: "mr-3" %>
              Logout
            <% end %>
          </div>
        </div>
      </div>
    </div>

    <!-- Mobile menu button -->
    <div class="lg:hidden fixed top-4 left-4 z-50">
      <button @click="sidebarOpen = true" class="glass p-3 rounded-xl shadow-lg hover:shadow-xl transition-all duration-200">
        <%= heroicon_tag "bars-3", size: 24, class: "text-gray-700" %>
      </button>
    </div>

    <!-- Main content -->
    <div class="flex flex-col flex-1 overflow-hidden">
      <!-- Enhanced Top bar -->
      <div class="glass border-b border-white/20 shadow-sm">
        <div class="px-4 sm:px-6 lg:px-8">
          <div class="flex items-center justify-between h-16">
            <div class="flex items-center space-x-4">
              <h1 class="text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-600 bg-clip-text text-transparent">Dashboard</h1>
              <div class="hidden sm:flex items-center space-x-2">
                <div class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></div>
                <span class="text-sm text-gray-600">Live</span>
              </div>
            </div>
            <div class="flex items-center space-x-4">
              <!-- Quick Actions -->
              <div class="hidden md:flex items-center space-x-2">
                <a href="/admin/posts/new" class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-indigo-500 to-purple-600 text-white text-sm font-medium rounded-lg hover:from-indigo-600 hover:to-purple-700 transition-all duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5">
                  <%= heroicon_tag "plus", size: 20, class: "mr-2" %>
                  New Post
                </a>
              </div>
              
              <!-- User Avatar -->
              <div class="flex items-center space-x-3">
                <div class="hidden sm:block text-right">
                  <p class="text-sm font-medium text-gray-900">Welcome back!</p>
                  <p class="text-xs text-gray-500"><%= Time.current.strftime("%B %d, %Y") %></p>
                </div>
                <div class="w-10 h-10 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-full flex items-center justify-center text-white font-semibold shadow-lg">
                  <%= current_admin_user.email.first.upcase %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Dashboard content -->
      <main class="flex-1 relative overflow-y-auto focus:outline-none custom-scrollbar">
        <div class="py-8">
          <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <!-- Welcome Section -->
            <div class="mb-8 animate-fade-in-up">
              <h2 class="text-3xl font-bold bg-gradient-to-r from-gray-900 via-indigo-800 to-purple-800 bg-clip-text text-transparent mb-2">
                Good <%= Time.current.hour < 12 ? 'morning' : Time.current.hour < 18 ? 'afternoon' : 'evening' %>, <%= current_admin_user&.email&.split('@')&.first&.capitalize || 'Admin' %>!
              </h2>
              <p class="text-lg text-gray-600">Here's what's happening with your blog today</p>
            </div>

            <!-- Enhanced Stats Grid -->
            <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4 mb-8 animate-fade-in-up" style="animation-delay: 0.1s">
              <!-- Total Posts -->
              <div class="metric-card glass overflow-hidden shadow-lg rounded-2xl border border-white/20">
                <div class="p-6">
                  <div class="flex items-center justify-between">
                    <div>
                      <p class="text-sm font-medium text-gray-600 uppercase tracking-wide">Total Posts</p>
                      <p class="text-3xl font-bold text-gray-900 mt-2"><%= @total_posts %></p>
                      <% if @posts_growth != 0 %>
                        <p class="text-sm mt-2 flex items-center">
                          <% if @posts_growth > 0 %>
                            <%= heroicon_tag "arrow-trending-up", size: 20, class: "text-green-500 mr-1" %>
                            <span class="text-green-600 font-medium">+<%= @posts_growth %>%</span>
                          <% else %>
                            <%= heroicon_tag "arrow-trending-down", size: 20, class: "text-red-500 mr-1" %>
                            <span class="text-red-600 font-medium"><%= @posts_growth %>%</span>
                          <% end %>
                          <span class="text-gray-500 ml-1">vs last month</span>
                        </p>
                      <% end %>
                    </div>
                    <div class="w-16 h-16 gradient-primary rounded-2xl flex items-center justify-center shadow-lg">
                      <%= heroicon_tag "document-text", size: 24, class: "text-white" %>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Published Posts -->
              <div class="metric-card glass overflow-hidden shadow-lg rounded-2xl border border-white/20">
                <div class="p-6">
                  <div class="flex items-center justify-between">
                    <div>
                      <p class="text-sm font-medium text-gray-600 uppercase tracking-wide">Published</p>
                      <p class="text-3xl font-bold text-gray-900 mt-2"><%= @published_posts %></p>
                      <p class="text-sm text-gray-500 mt-2">
                        <%= @published_this_week %> this week
                      </p>
                    </div>
                    <div class="w-16 h-16 gradient-success rounded-2xl flex items-center justify-center shadow-lg">
                      <%= heroicon_tag "check-circle", size: 24, class: "text-white" %>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Draft Posts -->
              <div class="metric-card glass overflow-hidden shadow-lg rounded-2xl border border-white/20">
                <div class="p-6">
                  <div class="flex items-center justify-between">
                    <div>
                      <p class="text-sm font-medium text-gray-600 uppercase tracking-wide">Drafts</p>
                      <p class="text-3xl font-bold text-gray-900 mt-2"><%= @draft_posts %></p>
                      <% if @draft_posts > 0 %>
                        <a href="/admin/posts?scope=draft" class="text-sm text-amber-600 hover:text-amber-700 font-medium mt-2 inline-block">
                          Review drafts →
                        </a>
                      <% else %>
                        <p class="text-sm text-gray-500 mt-2">All caught up!</p>
                      <% end %>
                    </div>
                    <div class="w-16 h-16 gradient-warning rounded-2xl flex items-center justify-center shadow-lg">
                      <%= heroicon_tag "pencil-square", size: 24, class: "text-white" %>
                    </div>
                  </div>
                </div>
              </div>

              <!-- Total Views -->
              <div class="metric-card glass overflow-hidden shadow-lg rounded-2xl border border-white/20">
                <div class="p-6">
                  <div class="flex items-center justify-between">
                    <div>
                      <p class="text-sm font-medium text-gray-600 uppercase tracking-wide">Total Views</p>
                      <p class="text-3xl font-bold text-gray-900 mt-2"><%= number_with_delimiter(@total_views) %></p>
                      <p class="text-sm text-gray-500 mt-2">
                        <%= @total_reading_time %> min reading time
                      </p>
                    </div>
                    <div class="w-16 h-16 gradient-info rounded-2xl flex items-center justify-center shadow-lg">
                      <%= heroicon_tag "eye", size: 24, class: "text-white" %>
                    </div>
                  </div>
                </div>
              </div>
            </div>

            <!-- Main Content Grid -->
            <div class="grid grid-cols-1 xl:grid-cols-3 gap-8">
              <!-- Enhanced Activity Feed -->
              <div class="xl:col-span-2 animate-fade-in-up" style="animation-delay: 0.2s">
                <div class="glass shadow-lg rounded-2xl border border-white/20 overflow-hidden">
                  <div class="px-6 py-5 border-b border-gray-100/50">
                    <div class="flex items-center justify-between">
                      <h3 class="text-lg font-semibold text-gray-900 flex items-center">
                        <%= heroicon_tag "bolt", size: 20, class: "mr-2 text-indigo-500" %>
                        Recent Activity
                      </h3>
                      <a href="/admin/posts" class="text-sm text-indigo-600 hover:text-indigo-700 font-medium">View all</a>
                    </div>
                  </div>
                  <div class="p-6">
                    <% if @recent_activity.any? %>
                      <div class="space-y-6">
                        <% @recent_activity.each_with_index do |activity, index| %>
                          <div class="timeline-item relative pl-8 <%= 'pb-6' unless index == @recent_activity.length - 1 %>">
                            <div class="absolute left-0 top-0 w-8 h-8 rounded-full flex items-center justify-center
                                        <%= activity[:type] == 'published' ? 'bg-green-100 text-green-600' : 'bg-yellow-100 text-yellow-600' %> shadow-md">
                              <% if activity[:type] == 'published' %>
                                <%= heroicon_tag "check-circle", size: 20 %>
                              <% else %>
                                <%= heroicon_tag "pencil-square", size: 20 %>
                              <% end %>
                            </div>
                            <div class="bg-white/50 rounded-xl p-4 hover:bg-white/70 transition-all duration-200 border border-gray-100/50">
                              <div class="flex items-start justify-between">
                                <div class="flex-1">
                                  <p class="text-sm font-semibold text-gray-900 mb-1">
                                    <a href="/admin/posts/<%= activity[:slug] %>" class="hover:text-indigo-600 transition-colors">
                                      <%= activity[:title] %>
                                    </a>
                                  </p>
                                  <div class="flex items-center space-x-4 text-xs text-gray-500">
                                    <span>
                                      <%= activity[:type] == 'published' ? 'Published' : 'Updated' %> 
                                      <%= time_ago_in_words(activity[:time]) %> ago
                                    </span>
                                    <span class="flex items-center">
                                      <%= heroicon_tag "clock", size: 20, class: "mr-1" %>
                                      <%= activity[:reading_time] %> min read
                                    </span>
                                  </div>
                                </div>
                                <div class="flex items-center space-x-2 ml-4">
                                  <a href="/admin/posts/<%= activity[:slug] %>" 
                                     class="inline-flex items-center px-3 py-1 text-xs font-medium bg-indigo-100 text-indigo-700 rounded-full hover:bg-indigo-200 transition-colors">
                                    Edit
                                  </a>
                                </div>
                              </div>
                            </div>
                          </div>
                        <% end %>
                      </div>
                    <% else %>
                      <div class="text-center py-16">
                        <div class="w-16 h-16 mx-auto mb-4 bg-gray-100 rounded-full flex items-center justify-center">
                          <%= heroicon_tag "document-text", size: 24, class: "text-gray-400" %>
                        </div>
                        <h3 class="text-lg font-medium text-gray-900 mb-2">No posts yet</h3>
                        <p class="text-gray-500 mb-6">Create your first post to get started with your blog!</p>
                        <a href="/admin/posts/new" 
                           class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-indigo-500 to-purple-600 text-white font-medium rounded-xl hover:from-indigo-600 hover:to-purple-700 transition-all duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5">
                          <%= heroicon_tag "plus", size: 20, class: "mr-2" %>
                          Create Your First Post
                        </a>
                      </div>
                    <% end %>
                  </div>
                </div>
              </div>

              <!-- Enhanced Sidebar -->
              <div class="space-y-6 animate-fade-in-up" style="animation-delay: 0.3s">
                <!-- Quick Actions -->
                <div class="glass shadow-lg rounded-2xl border border-white/20 overflow-hidden">
                  <div class="px-6 py-5 border-b border-gray-100/50">
                    <h3 class="text-lg font-semibold text-gray-900 flex items-center">
                      <%= heroicon_tag "bolt", size: 20, class: "mr-2 text-indigo-500" %>
                      Quick Actions
                    </h3>
                  </div>
                  <div class="p-6 space-y-3">
                    <a href="/admin/posts/new" 
                       class="flex items-center justify-center w-full bg-gradient-to-r from-indigo-500 to-purple-600 text-white px-4 py-3 rounded-xl font-medium hover:from-indigo-600 hover:to-purple-700 transition-all duration-200 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5">
                      <%= heroicon_tag "plus", size: 20, class: "mr-2" %>
                      New Post
                    </a>
                    
                    <a href="/admin/posts?scope=draft" 
                       class="flex items-center justify-between w-full bg-amber-50 hover:bg-amber-100 text-amber-800 px-4 py-3 rounded-xl font-medium transition-all duration-200 border border-amber-200">
                      <div class="flex items-center">
                        <%= heroicon_tag "pencil-square", size: 20, class: "mr-2" %>
                        View Drafts
                      </div>
                      <span class="bg-amber-200 text-amber-800 text-xs font-bold px-2 py-1 rounded-full"><%= @draft_posts %></span>
                    </a>
                    
                    <a href="/admin/posts" 
                       class="flex items-center justify-center w-full bg-green-50 hover:bg-green-100 text-green-800 px-4 py-3 rounded-xl font-medium transition-all duration-200 border border-green-200">
                      <%= heroicon_tag "document-text", size: 20, class: "mr-2" %>
                      All Posts
                    </a>
                    
                    <button onclick="publishSite()" 
                            class="flex items-center justify-center w-full bg-blue-50 hover:bg-blue-100 text-blue-800 px-4 py-3 rounded-xl font-medium transition-all duration-200 border border-blue-200">
                      <%= heroicon_tag "cloud-arrow-down", size: 20, class: "mr-2" %>
                      Publish Site
                    </button>
                  </div>
                </div>

                <!-- Content Statistics -->
                <div class="glass shadow-lg rounded-2xl border border-white/20 overflow-hidden">
                  <div class="px-6 py-5 border-b border-gray-100/50">
                    <h3 class="text-lg font-semibold text-gray-900 flex items-center">
                      <%= heroicon_tag "chart-bar", size: 20, class: "mr-2 text-indigo-500" %>
                      Content Overview
                    </h3>
                  </div>
                  <div class="p-6 space-y-4">
                    <div class="text-center p-4 bg-gradient-to-br from-blue-50 to-indigo-50 rounded-xl border border-blue-100">
                      <div class="text-2xl font-bold text-blue-900 mb-1"><%= number_with_delimiter(@total_words) %></div>
                      <div class="text-sm text-blue-600 font-medium">Total Words Written</div>
                      <div class="text-xs text-blue-500 mt-1">Across all published posts</div>
                    </div>
                    
                    <div class="text-center p-4 bg-gradient-to-br from-green-50 to-emerald-50 rounded-xl border border-green-100">
                      <div class="text-2xl font-bold text-green-900 mb-1"><%= @avg_reading_time %> min</div>
                      <div class="text-sm text-green-600 font-medium">Avg Reading Time</div>
                      <div class="text-xs text-green-500 mt-1">Per published post</div>
                    </div>
                    
                    <div class="text-center p-4 bg-gradient-to-br from-purple-50 to-pink-50 rounded-xl border border-purple-100">
                      <div class="text-2xl font-bold text-purple-900 mb-1"><%= @posts_this_month %></div>
                      <div class="text-sm text-purple-600 font-medium">Posts This Month</div>
                      <div class="text-xs text-purple-500 mt-1"><%= Date.current.strftime("%B %Y") %></div>
                    </div>
                  </div>
                </div>

                <!-- Security & Rate Limiting Status -->
                <% if @security_stats %>
                  <div class="glass shadow-lg rounded-2xl border border-white/20 overflow-hidden">
                    <div class="px-6 py-5 border-b border-gray-100/50">
                      <h3 class="text-lg font-semibold text-gray-900 flex items-center">
                        <%= heroicon_tag "shield-check", size: 20, class: "mr-2 text-indigo-500" %>
                        Security Status
                      </h3>
                    </div>
                    <div class="p-6">
                      <% if @security_stats[:rack_attack_enabled] %>
                        <div class="space-y-4">
                          <!-- Rate Limiting Status -->
                          <div class="flex items-center justify-between p-3 bg-green-50 rounded-lg border border-green-100">
                            <div class="flex items-center">
                              <%= heroicon_tag "check-circle", size: 16, class: "mr-2 text-green-600" %>
                              <span class="text-sm font-medium text-green-800">Rate Limiting Active</span>
                            </div>
                            <div class="text-xs text-green-600 font-medium">
                              <%= @security_stats[:active_throttles] %> rules
                            </div>
                          </div>
                          
                          <!-- Cache Store Info -->
                          <div class="text-center p-3 bg-blue-50 rounded-lg border border-blue-100">
                            <div class="text-sm font-medium text-blue-800 mb-1">Cache Store</div>
                            <div class="text-xs text-blue-600"><%= @security_stats[:cache_store].split('::').last %></div>
                          </div>
                          
                          <!-- Protection Summary -->
                          <div class="grid grid-cols-3 gap-2 text-center">
                            <div class="p-2 bg-purple-50 rounded border border-purple-100">
                              <div class="text-sm font-bold text-purple-900"><%= @security_stats[:active_throttles] %></div>
                              <div class="text-xs text-purple-600">Throttles</div>
                            </div>
                            <div class="p-2 bg-red-50 rounded border border-red-100">
                              <div class="text-sm font-bold text-red-900"><%= @security_stats[:active_blocklists] %></div>
                              <div class="text-xs text-red-600">Blocklists</div>
                            </div>
                            <div class="p-2 bg-green-50 rounded border border-green-100">
                              <div class="text-sm font-bold text-green-900"><%= @security_stats[:active_safelists] %></div>
                              <div class="text-xs text-green-600">Safelists</div>
                            </div>
                          </div>
                          
                          <!-- Recent Security Events -->
                          <% if @security_stats[:recent_events]&.any? %>
                            <div class="border-t border-gray-100/50 pt-4">
                              <div class="text-sm font-medium text-gray-700 mb-3">Recent Events</div>
                              <div class="space-y-2 max-h-32 overflow-y-auto custom-scrollbar">
                                <% @security_stats[:recent_events].first(3).each do |event| %>
                                  <div class="text-xs p-2 bg-gray-50 rounded border border-gray-100">
                                    <div class="flex items-center justify-between mb-1">
                                      <span class="font-medium text-gray-700"><%= event[:severity] %></span>
                                      <span class="text-gray-500"><%= event[:timestamp] %></span>
                                    </div>
                                    <div class="text-gray-600 truncate"><%= event[:message] %></div>
                                  </div>
                                <% end %>
                              </div>
                            </div>
                          <% end %>
                        </div>
                      <% else %>
                        <div class="text-center p-4 bg-yellow-50 rounded-lg border border-yellow-100">
                          <div class="flex items-center justify-center mb-2">
                            <%= heroicon_tag "exclamation-triangle", size: 20, class: "text-yellow-600" %>
                          </div>
                          <div class="text-sm font-medium text-yellow-800 mb-1">Rate Limiting Disabled</div>
                          <div class="text-xs text-yellow-700">Security protection inactive</div>
                          <% if @security_stats[:error] %>
                            <div class="text-xs text-red-600 mt-2 font-mono"><%= @security_stats[:error] %></div>
                          <% end %>
                        </div>
                      <% end %>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  </div>

  <!-- Mobile sidebar overlay -->
  <div x-show="sidebarOpen" 
       x-transition:enter="transition-opacity ease-linear duration-300"
       x-transition:enter-start="opacity-0"
       x-transition:enter-end="opacity-100"
       x-transition:leave="transition-opacity ease-linear duration-300"
       x-transition:leave-start="opacity-100"
       x-transition:leave-end="opacity-0"
       @click="sidebarOpen = false"
       class="fixed inset-0 bg-black bg-opacity-50 z-30 lg:hidden"
       style="display: none;"></div>

  <!-- Mobile sidebar -->
  <div x-show="sidebarOpen"
       x-transition:enter="transition ease-in-out duration-300 transform"
       x-transition:enter-start="-translate-x-full"
       x-transition:enter-end="translate-x-0"
       x-transition:leave="transition ease-in-out duration-300 transform"
       x-transition:leave-start="translate-x-0"
       x-transition:leave-end="-translate-x-full"
       class="fixed inset-y-0 left-0 z-40 w-64 lg:hidden"
       style="display: none;">
    <div class="flex flex-col flex-grow glass border-r border-white/20 pt-5 pb-4 overflow-y-auto shadow-xl">
      <!-- Mobile sidebar content (same as desktop) -->
      <div class="flex items-center flex-shrink-0 px-6 pb-6">
        <div class="flex items-center space-x-3">
          <div class="w-10 h-10 bg-gradient-to-br from-indigo-500 to-purple-600 rounded-xl flex items-center justify-center shadow-lg">
            <span class="text-white font-bold text-lg">R</span>
          </div>
          <div>
            <h1 class="text-xl font-bold text-gray-900">Railquill</h1>
            <p class="text-xs text-gray-500">Admin Dashboard</p>
          </div>
        </div>
      </div>
      
      <nav class="flex-1 px-4 space-y-2">
        <a href="/admin_dashboard" @click="sidebarOpen = false" class="bg-white/60 text-indigo-700 group flex items-center px-4 py-3 text-sm font-medium rounded-xl border-l-4 border-indigo-500 shadow-sm">
          <%= heroicon_tag "squares-2x2", size: 20, class: "mr-3" %>
          Dashboard
        </a>
        <a href="/admin/posts" @click="sidebarOpen = false" class="text-gray-700 hover:bg-white/40 hover:text-indigo-600 group flex items-center px-4 py-3 text-sm font-medium rounded-xl transition-all duration-200">
          <%= heroicon_tag "pencil-square", size: 20, class: "mr-3" %>
          Posts
        </a>
      </nav>
    </div>
  </div>

  <script>
    function publishSite() {
      // Show loading state
      const button = event.target;
      const originalContent = button.innerHTML;
      button.innerHTML = `
        <div class="w-5 h-5 mr-2 animate-spin border-2 border-white border-t-transparent rounded-full"></div>
        Publishing...
      `;
      button.disabled = true;
      
      // Simulate publish process (replace with actual implementation)
      setTimeout(() => {
        button.innerHTML = `
          <div class="w-5 h-5 mr-2 text-white rounded-full bg-green-500 flex items-center justify-center">✓</div>
          Published!
        `;
        
        setTimeout(() => {
          button.innerHTML = originalContent;
          button.disabled = false;
        }, 2000);
      }, 2000);
    }
    
    // Add smooth scroll behavior
    document.addEventListener('DOMContentLoaded', function() {
      // Initialize any additional interactivity here
      console.log('Railquill Admin Dashboard loaded successfully!');
    });
  </script>
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  
  <title><%= [@page_title, active_admin_config.site_title].compact.join(" | ") %></title>
  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['Inter', 'system-ui', 'sans-serif'],
          }
        }
      }
    }
  </script>
  
  <!-- Inter Font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  
  <!-- Custom styles -->
  <style>
    body { font-family: 'Inter', sans-serif; }
    
    /* Hide default ActiveAdmin styling */
    .header, #title_bar, #footer, .breadcrumb { display: none !important; }
    
    /* Custom scrollbar */
    ::-webkit-scrollbar {
      width: 8px;
    }
    ::-webkit-scrollbar-track {
      background: #f1f5f9;
    }
    ::-webkit-scrollbar-thumb {
      background: #cbd5e1;
      border-radius: 4px;
    }
    ::-webkit-scrollbar-thumb:hover {
      background: #94a3b8;
    }
    
    /* Animation */
    .slide-in {
      animation: slideIn 0.3s ease-out;
    }
    
    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    /* Form Styles */
    .form_with {
      max-width: none;
    }
    
    fieldset {
      background: white;
      border-radius: 0.5rem;
      border: 1px solid #e5e7eb;
      box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
      margin-bottom: 1.5rem;
    }
    
    fieldset legend {
      padding: 0.5rem 1rem;
      font-weight: 600;
      color: #111827;
      background: #f9fafb;
      border-bottom: 1px solid #e5e7eb;
      width: 100%;
      margin-bottom: 0;
      border-radius: 0.5rem 0.5rem 0 0;
    }
    
    fieldset ol {
      padding: 1.5rem;
      list-style: none;
      margin: 0;
    }
    
    fieldset ol li {
      margin-bottom: 1.5rem;
    }
    
    fieldset ol li label {
      display: block;
      font-size: 0.875rem;
      font-weight: 500;
      color: #374151;
      margin-bottom: 0.25rem;
    }
    
    fieldset ol li input[type="text"],
    fieldset ol li input[type="email"],
    fieldset ol li input[type="password"],
    fieldset ol li input[type="url"],
    fieldset ol li input[type="datetime-local"],
    fieldset ol li select,
    fieldset ol li textarea {
      width: 100%;
      padding: 0.5rem 0.75rem;
      border: 1px solid #d1d5db;
      border-radius: 0.375rem;
      box-shadow: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
      font-size: 0.875rem;
      line-height: 1.5;
    }
    
    fieldset ol li input[type="text"]:focus,
    fieldset ol li input[type="email"]:focus,
    fieldset ol li input[type="password"]:focus,
    fieldset ol li input[type="url"]:focus,
    fieldset ol li input[type="datetime-local"]:focus,
    fieldset ol li select:focus,
    fieldset ol li textarea:focus {
      outline: none;
      border-color: #6366f1;
      box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
    }
    
    fieldset ol li textarea {
      min-height: 8rem;
      resize: vertical;
    }
    
    fieldset ol li .hint {
      font-size: 0.75rem;
      color: #6b7280;
      margin-top: 0.25rem;
    }
    
    .form_actions {
      margin-top: 2rem;
      display: flex;
      gap: 0.75rem;
    }
    
    .form_actions input[type="submit"] {
      background: #4f46e5;
      color: white;
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      font-weight: 500;
      border: none;
      cursor: pointer;
      transition: background-color 0.2s;
    }
    
    .form_actions input[type="submit"]:hover {
      background: #4338ca;
    }
    
    .form_actions a {
      background: #f3f4f6;
      color: #374151;
      padding: 0.75rem 1.5rem;
      border-radius: 0.5rem;
      font-weight: 500;
      text-decoration: none;
      transition: background-color 0.2s;
      border: 1px solid #d1d5db;
    }
    
    .form_actions a:hover {
      background: #e5e7eb;
    }
  </style>
</head>

<body class="bg-gray-50 font-sans">
  <div class="flex min-h-screen">
    <!-- Sidebar -->
    <aside class="w-64 bg-white shadow-lg border-r border-gray-200 fixed h-full z-40 transform -translate-x-full lg:translate-x-0 transition-transform duration-300 ease-in-out" id="sidebar">
      <!-- Logo -->
      <div class="p-6 border-b border-gray-100">
        <h1 class="text-xl font-bold text-gray-900">
          <%= link_to admin_root_path, class: "flex items-center gap-3 hover:text-indigo-600 transition-colors" do %>
            <%= heroicon_tag "book-open", size: 24, class: "text-indigo-600" %>
            <span>Railquill</span>
          <% end %>
        </h1>
      </div>
      
      <!-- Navigation -->
      <nav class="p-4">
        <ul class="space-y-1">
          <li>
            <%= link_to admin_root_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg transition-colors #{request.path == admin_root_path || params[:controller] == 'admin/dashboard' ? 'bg-indigo-50 text-indigo-600 font-semibold' : 'text-gray-700 hover:bg-gray-100'}" do %>
              <%= heroicon_tag "chart-bar", size: 20, class: "" %>
              <span>Dashboard</span>
            <% end %>
          </li>
          <li>
            <%= link_to admin_posts_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg transition-colors #{params[:controller] == 'admin/posts' ? 'bg-indigo-50 text-indigo-600 font-semibold' : 'text-gray-700 hover:bg-gray-100'}" do %>
              <%= heroicon_tag "pencil-square", size: 20, class: "" %>
              <span>Posts</span>
            <% end %>
          </li>
          <li>
            <%= link_to admin_site_configs_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg transition-colors #{params[:controller] == 'admin/site_configs' ? 'bg-indigo-50 text-indigo-600 font-semibold' : 'text-gray-700 hover:bg-gray-100'}" do %>
              <%= heroicon_tag "cog-6-tooth", size: 20, class: "" %>
              <span>Site Settings</span>
            <% end %>
          </li>
          <li>
            <%= link_to admin_admin_users_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg transition-colors #{params[:controller] == 'admin/admin_users' ? 'bg-indigo-50 text-indigo-600 font-semibold' : 'text-gray-700 hover:bg-gray-100'}" do %>
              <%= heroicon_tag "user-group", size: 20, class: "" %>
              <span>Admin Users</span>
            <% end %>
          </li>
        </ul>
        
        <!-- Logout -->
        <div class="pt-4 mt-4 border-t border-gray-200">
          <%= link_to destroy_admin_user_session_path, method: :delete, 
                      class: "flex items-center gap-3 px-4 py-3 rounded-lg text-gray-700 hover:bg-red-50 hover:text-red-600 transition-colors" do %>
            <%= heroicon_tag "arrow-right-on-rectangle", size: 20, class: "" %>
            <span>Logout</span>
          <% end %>
        </div>
      </nav>
    </aside>
    
    <!-- Mobile Menu Button -->
    <button id="mobile-menu-button" class="lg:hidden fixed top-4 left-4 z-50 bg-white rounded-lg p-2 shadow-md border border-gray-200 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-indigo-500">
      <%= heroicon_tag "bars-3", size: 24, class: "text-gray-700" %>
    </button>
    
    <!-- Main Content -->
    <main class="flex-1 lg:ml-64">
      <!-- Top Bar -->
      <div class="bg-white border-b border-gray-200 px-4 lg:px-8 py-4">
        <div class="flex items-center justify-between">
          <h1 class="text-2xl font-bold text-gray-900">
            <%= @page_title || assigns[:page_title] || 
                case params[:controller]
                when 'admin/dashboard' then 'Dashboard'
                when 'admin/posts' then 'Posts'
                when 'admin/admin_users' then 'Admin Users'
                when 'admin/site_configs' then 'Site Settings'
                else params[:controller]&.humanize
                end %>
          </h1>
          <div class="flex items-center gap-4">
            <% if current_admin_user %>
              <div class="flex items-center gap-2 text-sm text-gray-600">
                <span class="w-8 h-8 bg-indigo-100 text-indigo-600 rounded-full flex items-center justify-center font-medium">
                  <%= current_admin_user.email.first.upcase %>
                </span>
                <span>Welcome, <%= current_admin_user.email.split('@').first.capitalize %></span>
              </div>
            <% end %>
          </div>
        </div>
      </div>
      
      <!-- Page Content -->
      <div class="p-4 lg:p-8">
        <!-- Flash Messages -->
        <% flash.each do |type, message| %>
          <% next if message.blank? %>
          <div class="mb-6 p-4 rounded-lg flex items-center gap-3 slide-in <%= type == 'notice' ? 'bg-green-50 border border-green-200 text-green-700' : 'bg-red-50 border border-red-200 text-red-700' %>">
            <% if type == 'notice' %>
              <%= heroicon_tag "check-circle", size: 20, class: "text-green-500" %>
            <% else %>
              <%= heroicon_tag "exclamation-circle", size: 20, class: "text-red-500" %>
            <% end %>
            <%= message %>
          </div>
        <% end %>
        
        <!-- Main Content -->
        <div class="slide-in">
          <%= yield %>
        </div>
      </div>
      
      <!-- Footer -->
      <footer class="bg-white border-t border-gray-200 px-4 lg:px-8 py-6 mt-8">
        <div class="text-center text-gray-500 text-sm">
          <p>&copy; <%= Date.current.year %> Railquill Admin Panel</p>
        </div>
      </footer>  
    </main>
  </div>
  
  <!-- Overlay for mobile -->
  <div id="mobile-overlay" class="fixed inset-0 bg-black bg-opacity-50 z-30 hidden lg:hidden"></div>
  
  <script>
    // Mobile menu functionality
    const mobileMenuButton = document.getElementById('mobile-menu-button');
    const sidebar = document.getElementById('sidebar');
    const overlay = document.getElementById('mobile-overlay');
    
    function toggleMobileMenu() {
      sidebar.classList.toggle('-translate-x-full');
      overlay.classList.toggle('hidden');
    }
    
    mobileMenuButton.addEventListener('click', toggleMobileMenu);
    overlay.addEventListener('click', toggleMobileMenu);
    
    // Close mobile menu on window resize
    window.addEventListener('resize', function() {
      if (window.innerWidth >= 1024) {
        sidebar.classList.remove('-translate-x-full');
        overlay.classList.add('hidden');
      }
    });
  </script>
</body>
</html>
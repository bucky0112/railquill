<!DOCTYPE html>
<html lang="en">
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  
  <title><%= [@page_title, active_admin_config.site_title].compact.join(" | ") %></title>
  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <!-- Tailwind CSS -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          fontFamily: {
            sans: ['Inter', 'system-ui', 'sans-serif'],
          }
        }
      }
    }
  </script>
  
  <!-- Inter Font -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  
  <!-- Custom styles -->
  <style>
    body { font-family: 'Inter', sans-serif; }
    
    /* Hide default ActiveAdmin styling */
    .header, #title_bar, #footer, .breadcrumb { display: none !important; }
    
    /* Custom scrollbar */
    ::-webkit-scrollbar {
      width: 8px;
    }
    ::-webkit-scrollbar-track {
      background: #f1f5f9;
    }
    ::-webkit-scrollbar-thumb {
      background: #cbd5e1;
      border-radius: 4px;
    }
    ::-webkit-scrollbar-thumb:hover {
      background: #94a3b8;
    }
    
    /* Animation */
    .slide-in {
      animation: slideIn 0.3s ease-out;
    }
    
    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateY(10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>

<body class="bg-gray-50 font-sans">
  <div class="flex min-h-screen">
    <!-- Sidebar -->
    <aside class="w-64 bg-white shadow-lg border-r border-gray-200 fixed h-full z-40 transform -translate-x-full lg:translate-x-0 transition-transform duration-300 ease-in-out" id="sidebar">
      <!-- Logo -->
      <div class="p-6 border-b border-gray-100">
        <h1 class="text-xl font-bold text-gray-900">
          <%= link_to admin_root_path, class: "flex items-center gap-3 hover:text-indigo-600 transition-colors" do %>
            <span class="text-2xl">📚</span>
            <span>Railquill</span>
          <% end %>
        </h1>
      </div>
      
      <!-- Navigation -->
      <nav class="p-4">
        <ul class="space-y-1">
          <li>
            <%= link_to admin_root_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg transition-colors #{request.path == admin_root_path || params[:controller] == 'admin/dashboard' ? 'bg-indigo-50 text-indigo-600 font-semibold' : 'text-gray-700 hover:bg-gray-100'}" do %>
              <span class="text-xl">📊</span>
              <span>Dashboard</span>
            <% end %>
          </li>
          <li>
            <%= link_to admin_posts_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg transition-colors #{params[:controller] == 'admin/posts' ? 'bg-indigo-50 text-indigo-600 font-semibold' : 'text-gray-700 hover:bg-gray-100'}" do %>
              <span class="text-xl">📝</span>
              <span>Posts</span>
            <% end %>
          </li>
          <li>
            <%= link_to admin_admin_users_path, class: "flex items-center gap-3 px-4 py-3 rounded-lg transition-colors #{params[:controller] == 'admin/admin_users' ? 'bg-indigo-50 text-indigo-600 font-semibold' : 'text-gray-700 hover:bg-gray-100'}" do %>
              <span class="text-xl">👥</span>
              <span>Admin Users</span>
            <% end %>
          </li>
        </ul>
        
        <!-- Logout -->
        <div class="pt-4 mt-4 border-t border-gray-200">
          <%= link_to destroy_admin_user_session_path, method: :delete, 
                      class: "flex items-center gap-3 px-4 py-3 rounded-lg text-gray-700 hover:bg-red-50 hover:text-red-600 transition-colors" do %>
            <span class="text-xl">🚪</span>
            <span>Logout</span>
          <% end %>
        </div>
      </nav>
    </aside>
    
    <!-- Mobile Menu Button -->
    <button id="mobile-menu-button" class="lg:hidden fixed top-4 left-4 z-50 bg-white rounded-lg p-2 shadow-md border border-gray-200 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-indigo-500">
      <svg class="w-6 h-6 text-gray-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
      </svg>
    </button>
    
    <!-- Main Content -->
    <main class="flex-1 lg:ml-64">
      <!-- Top Bar -->
      <div class="bg-white border-b border-gray-200 px-4 lg:px-8 py-4">
        <div class="flex items-center justify-between">
          <h1 class="text-2xl font-bold text-gray-900">
            <%= @page_title || assigns[:page_title] || 
                case params[:controller]
                when 'admin/dashboard' then 'Dashboard'
                when 'admin/posts' then 'Posts'
                when 'admin/admin_users' then 'Admin Users'
                else params[:controller]&.humanize
                end %>
          </h1>
          <div class="flex items-center gap-4">
            <% if current_admin_user %>
              <div class="flex items-center gap-2 text-sm text-gray-600">
                <span class="w-8 h-8 bg-indigo-100 text-indigo-600 rounded-full flex items-center justify-center font-medium">
                  <%= current_admin_user.email.first.upcase %>
                </span>
                <span>Welcome, <%= current_admin_user.email.split('@').first.capitalize %></span>
              </div>
            <% end %>
          </div>
        </div>
      </div>
      
      <!-- Page Content -->
      <div class="p-4 lg:p-8">
        <!-- Flash Messages -->
        <% flash.each do |type, message| %>
          <% next if message.blank? %>
          <div class="mb-6 p-4 rounded-lg flex items-center gap-3 slide-in <%= type == 'notice' ? 'bg-green-50 border border-green-200 text-green-700' : 'bg-red-50 border border-red-200 text-red-700' %>">
            <% if type == 'notice' %>
              <svg class="w-5 h-5 text-green-500" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
              </svg>
            <% else %>
              <svg class="w-5 h-5 text-red-500" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
              </svg>
            <% end %>
            <%= message %>
          </div>
        <% end %>
        
        <!-- Main Content -->
        <div class="slide-in">
          <%= yield %>
        </div>
      </div>
      
      <!-- Footer -->
      <footer class="bg-white border-t border-gray-200 px-4 lg:px-8 py-6 mt-8">
        <div class="text-center text-gray-500 text-sm">
          <p>&copy; <%= Date.current.year %> Railquill Admin Panel</p>
        </div>
      </footer>  
    </main>
  </div>
  
  <!-- Overlay for mobile -->
  <div id="mobile-overlay" class="fixed inset-0 bg-black bg-opacity-50 z-30 hidden lg:hidden"></div>
  
  <script>
    // Mobile menu functionality
    const mobileMenuButton = document.getElementById('mobile-menu-button');
    const sidebar = document.getElementById('sidebar');
    const overlay = document.getElementById('mobile-overlay');
    
    function toggleMobileMenu() {
      sidebar.classList.toggle('-translate-x-full');
      overlay.classList.toggle('hidden');
    }
    
    mobileMenuButton.addEventListener('click', toggleMobileMenu);
    overlay.addEventListener('click', toggleMobileMenu);
    
    // Close mobile menu on window resize
    window.addEventListener('resize', function() {
      if (window.innerWidth >= 1024) {
        sidebar.classList.remove('-translate-x-full');
        overlay.classList.add('hidden');
      }
    });
  </script>
</body>
</html>